# PythonProject – Retail ETL Pipeline

## Overview
Simple ETL from MySQL → PostgreSQL using Python.

## Requirements
- Docker (to run MySQL & PostgreSQL)
- Python 3.9+
- Python packages in `requirements.txt`

## Setup

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
